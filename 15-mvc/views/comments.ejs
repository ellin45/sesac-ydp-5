<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MVC 패턴</title>
</head>
<body>
    
    <h1>댓글모음</h1>
    <a href="/">홈으로 이동하기</a>

    <ul>
        <% for(let cmt of comments) { %>
           
    <li><b><%= cmt.userid %></b> - <%= cmt.comments %> <a href="/comment/<%= cmt.id %>"><%= cmt.comment %></a></li>
    <% } %>
    
    </ul>
</body>
</html>